storage:
 value: default
 wizard:
   name: "Storage Configuration"
   kind: cluster_storage

data_path: data

tensorflow_version:
  value: "1.5.0"
  wizard:
    name: "Tensorflow version"
    kind: select
    options:
    - "1.2.0"
    - "1.3.0"
    - "1.4.0"
    - "1.5.0"

python_version:
  value: "2"
  wizard:
    name: "Python version"
    kind: select
    options:
    - "2"
    - "3"

serving_port: 9000

tf_serving: false

